import type { CSSProperties } from "react";

/** Hijau sawah — inline di root DayPicker supaya tidak tertimpa style.css (biru). */
export const DAY_PICKER_BRAND_STYLE = {
  "--rdp-accent-color": "#445f24",
  "--rdp-accent-background-color": "rgba(68, 95, 36, 0.18)",
  "--rdp-today-color": "#445f24",
} as CSSProperties;
